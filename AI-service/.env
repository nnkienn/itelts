# mock = FREE (không tốn tiền); real = gọi GPT thật
MODE=mock

# bảo vệ nội bộ giữa Nest ↔ Python
AI_INTERNAL_TOKEN=super-secret

# model (chỉ dùng khi MODE=real)
OPENAI_API_KEY=sk-...
GPT_MODEL=gpt-4o-mini

# version & calibration (điều chỉnh band một chút cho “mượt”)
MODEL_VERSION=w-2025.08.1
CALIB_A=0.95
CALIB_B=0.20
